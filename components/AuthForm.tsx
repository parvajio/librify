import React from 'react'

// interface props<T extends FormField>

const AuthForm = ({type, schema, defaultvalue, onsubmit}:props) => {
  return (
    <div>AuthForm</div>
  )
}

export default AuthForm